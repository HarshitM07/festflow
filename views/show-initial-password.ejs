<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Coordinator Credentials - FestFlow</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center px-4">

  <div class="bg-gray-800 border border-gray-700 rounded-xl shadow-lg p-6 w-full max-w-lg">
    <h1 class="text-2xl font-bold mb-4 text-center">
      Coordinator Account Created
    </h1>

    <p class="text-sm text-gray-300 mb-4 text-center">
      Share these credentials with the coordinator securely. They will be forced to change their password on first login.
    </p>

    <div class="bg-gray-900 rounded-lg p-4 mb-4 border border-gray-700">
      <div class="mb-2">
        <span class="text-gray-400 text-sm">Club</span>
        <div class="font-semibold text-lg"><%= clubName %></div>
      </div>

      <div class="mb-2">
        <span class="text-gray-400 text-sm">Email</span>
        <div class="font-mono text-sm"><%= email %></div>
      </div>

      <div class="mb-2">
        <span class="text-gray-400 text-sm">Temporary Password</span>
        <div class="font-mono text-sm bg-gray-800 px-2 py-1 rounded inline-block">
          <%= password %>
        </div>
      </div>
    </div>

    <div class="text-xs text-yellow-300 mb-4">
      Note: The coordinator must log in with this password and will be redirected to the change password page on first login.
    </div>

    <form action="/superadmin/dashboard" method="GET" class="mt-4">
      <button
        type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 py-2 rounded-lg font-semibold"
      >
        Back to Dashboard
      </button>
    </form>
  </div>

</body>
</html>
