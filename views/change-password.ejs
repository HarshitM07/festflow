<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Change Password - FestFlow</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="bg-gray-900 text-white flex items-center justify-center h-screen px-4">

  <form action="/change-password" method="POST" class="bg-gray-800 p-8 rounded-lg shadow-md w-full max-w-md">

    <h1 class="text-2xl font-bold mb-6 text-center">Change Password</h1>

    <% if (query && query.error) { %>
      <div class="bg-red-500 text-white p-3 rounded mb-4 text-center font-medium shadow">
        <%= query.error %>
      </div>
    <% } %>

    <% if (query && query.success) { %>
      <div class="bg-green-500 text-white p-3 rounded mb-4 text-center font-medium shadow">
        <%= query.success %>
      </div>
    <% } %>

    <label class="block mb-4">
      <span class="text-sm mb-1 text-gray-300">Current Password</span>
      <input
        type="password"
        name="currentPassword"
        required
        class="w-full p-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </label>

    <label class="block mb-4">
      <span class="text-sm mb-1 text-gray-300">New Password</span>
      <input
        type="password"
        name="newPassword"
        required
        class="w-full p-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </label>

    <label class="block mb-6">
      <span class="text-sm mb-1 text-gray-300">Confirm New Password</span>
      <input
        type="password"
        name="confirmPassword"
        required
        class="w-full p-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
    </label>

    <button
      type="submit"
      class="w-full bg-green-600 hover:bg-green-700 p-2 rounded text-white font-semibold"
    >
      Update Password
    </button>
  </form>

</body>
</html>
