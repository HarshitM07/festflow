<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>View Registrants - <%= event.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen font-sans">
  <div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6 text-center">👥 Registrants for "<%= event.title %>"</h1>

    <% if (event.registrations.length === 0) { %>
      <p class="text-center text-gray-400 text-lg">No users have registered yet.</p>
    <% } else { %>
      <div class="bg-gray-800 rounded-lg p-6 shadow-lg">
        <table class="min-w-full table-auto">
          <thead>
            <tr class="text-left text-sm text-gray-400 border-b border-gray-700">
              <th class="pb-2">#</th>
              <th class="pb-2">Name</th>
              <th class="pb-2">Email</th>
            </tr>
          </thead>
          <tbody>
            <% event.registrations.forEach((user, index) => { %>
              <tr class="border-b border-gray-700 hover:bg-gray-700">
                <td class="py-2 pr-4"><%= index + 1 %></td>
                <td class="py-2 pr-4"><%= user.name %></td>
                <td class="py-2"><%= user.email %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>

    <div class="mt-6 text-center">
      <a href="/admin/my-events" class="text-blue-400 hover:underline">← Back to My Events</a>
    </div>
  </div>
</body>
</html>
